# GoodMemory plugin installed

Configure and activate it:

```bash
hermes memory setup goodmemory
hermes memory status
```

Then start a new Hermes session. The provider does not automatically upload
completed turns; durable writes happen only through explicit GoodMemory tools.
